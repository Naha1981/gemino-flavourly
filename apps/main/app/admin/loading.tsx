import { LoadingPanel } from '@/components/loading-panel';

/** RC5/F6 — Super Admin loading state. Pure static UI. */
export default function Loading() {
  return <LoadingPanel label="Loading admin console" />;
}
